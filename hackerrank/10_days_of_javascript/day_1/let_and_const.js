function main() {
    const PI= Math.PI;

    const r=readLine();
    r=parseFloat(r);
    // Print the area of the circle:
    console.log(PI*r*r);
    // Print the perimeter of the circle:
    console.log(2*PI*r);
}
